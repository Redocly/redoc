import { styled } from '../../styled-components.js';

export const Trigger = styled.div`
  display: inline-flex;
  align-items: center;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;
